# Research handoff implementation status (2026-09-22)

The 19-task comprehensive handoff is still in progress. The configured year-long run has **not** been launched. Its 2.72 TB security-only dense estimate is a nonmaterialized bound; the implementation uses measured compressed tiles and a bounded recomputable cache. Existing discovery artifacts retain their narrower legacy meaning.

| Tasks | Status | Evidence and remaining condition |
|---|---|---|
| 1–3 | Implemented | Explicit discovery scope, scientific/stage identities, sealed-period checks, and resolved scope publication. |
| 4 | Implemented for tested core path | Fused dual tiles now use stable logical pair membership, recorded identity and checksums, runtime subdivisions, and smaller retries on memory failure. A bounded gate completed 162/162 subgroup tasks with the new core path; direct numerical parity passed for different runtime pair caps. Closed orphan subgroup shards recover only after exact schema/key validation. |
| 5 | Implemented for numerical entrypoints | Shared owner lock and memory policy cover coverage, query workers, and feature worker admission. |
| 6–8 | Implemented for bounded coverage | Verified reader, causal group dictionaries, condition availability, exhaustive singles/duals planning independent of promotion, and fixed logical task IDs. Fine tails require exact raw feature data or a separate dependency. |
| 9 | Implemented for tested path | Security/fold compatibility tables are reduced from live shared moments during coverage, before those tiles can be evicted. A bounded real-core gate produced both tables; full-scale throughput remains unmeasured. |
| 10 | Implemented for bounded path | Dispersed tile sampling, measured density/compression, streaming reducer, LRU recomputable cache, catalog publication, snapshot pins, and recomputation. Full-scale throughput and sustained cache budget have not been measured. |
| 11–12 | Implemented for tested requests | Exact queries, stable query-bound cursors, full-scope search, durable job journal/retry/cancel and real worker handlers. Large production evidence remains unbuilt. |
| 13 | Implemented for bounded zoom | Multi-pair/multi-target scanning preserves request provenance and reuses one bounded packed feature tile across compatible target batches. A fresh real-core zoom gate executed 9/9 variant resolutions with zero failures, then published six candidates and six dossiers. Full-scale throughput remains unmeasured. |
| 14 | Implemented for independent diagnostics | Independent symbol/time/opportunity/raw-path diagnostics work on exact events; an on-demand dossier composes their result references. A fresh comprehensive `on_request` gate selected six candidates and built zero broad dossiers while completing 162/162 subgroup tasks. Fine tails report their exact raw-rank dependency as unavailable. |
| 15 | Implemented for isolated discovery replay | Raw and benchmark-adjusted execution replay use governed windows and explicit legs. A bounded production benchmark replay executed 138 trades; beta-residual replay correctly rejected 138 signals lacking causal beta. Portfolio replay remains a separate question. |
| 16 | Partial, measured | Feature waves use the admitted output budget; packed-bin alias hashes and timing telemetry are integrated. The 17-column gate spent 7.52 worker-seconds in feature construction versus 0.26 in panel reads, 0.044 in partition writes and 0.227 in scatter. A focused global-rank gate spent 1.823 seconds building versus 0.015 reading its panel, with one chunk and 430 MB peak process RSS; it had no overlapping-history reload to optimize. Target construction took about 1.45 seconds and the fused coarse dual scan 0.21 seconds. These bounded measurements do not justify more invasive staging, target, GPU-buffer or inference rewrites; multi-chunk/full-scale cost remains unmeasured. |
| 17 | Implemented for tested legacy/current paths | Comprehensive status distinguishes core, mandatory coverage and materialization; production exceptions publish a failed run status. The existing 3,026,670-row legacy temporal table remains readable by exact key and reports absent fold counts as typed NULL/unavailable, without inventing coverage. Metric definitions expose units, denominators and basis. |
| 18 | Partial gate passed | Focused reference/integration checks pass (44). Actual GPU parity and Windows lock checks pass. The bounded real-core comprehensive gate completed 162/162 mandatory tasks and the stored query → forced eviction → exact recomputation/search → diagnostic → neighbor → raw execution replay loop. Fresh zoom gates executed 9/9 variants under both legacy dossiers and comprehensive on-demand policy. A fresh stable-logical-tile core gate also completed 162/162 tasks. Additional specified same-workload before/after comparisons remain. |
| 19 | Partial | Request, scope, machine budget and Windows commands are documented. Mark ready only after Task 18 passes and all outstanding production conditions are resolved. |

## Local preflight

`configs/research/v3_comprehensive_20260922.yaml` resolves to the discovery year only. The local source catalog and RTX 3080 Ti exist. The last measured free output space was about 186.5 GiB after bounded gates. The security-only dense upper estimate was 2,721,032,244,000 bytes; it is **not** a required permanent allocation. The local preflight status is `admitted_streaming`. Populated density, compressed bytes and throughput cannot be known for the full request before core publication and representative tile sampling.

## Verification

`python -m pytest tests/unit/test_research_evidence_reference.py tests/integration/test_research_publication.py tests/unit/test_variant_batch_execution.py tests/unit/test_explicit_zoom.py tests/unit/test_production_repair.py tests/unit/test_cell_evidence.py -q` — 45 passed after the legacy adapter check. The integration fixture covers publication, interruption/resume, mandatory coverage, stored and recomputed query/search, diagnostics, neighbor job, raw replay, and worker handlers. CUDA moment parity and Windows numerical lock were exercised on this PC.

The separate `v3_comprehensive_gate` used the real core builder against governed local source data. It completed 162/162 mandatory subgroup tasks over all six grouping families and three resolutions. Representative tile compression was measured at 0.303 of dense bytes for this **small** gate; this ratio is not a full-run storage forecast. Its raw replay produced 138 executed trades and an inspectable rejection ledger, with an isolated net result of about -18.86%; that is a negative diagnostic result, not a strategy claim. The long full request was not launched. A warm resume took 19–29 seconds in the bounded gate, after the original 74-second run. A fresh `v3_comprehensive_gate_v2` run and warm resume retained the same evidence ID and one 162-task stage after the checkpoint fix.

A second bounded real-core hedge gate produced an isolated benchmark-adjusted replay with 138 executed trades and net additive trade return of about -38.24%; it is discovery-only evidence. Its beta-residual fixture lacked causal `beta_prior`, so all 138 attempted signals were rejected with that reason. A measured fixed search of seven matching tasks over 23,625 cells took 2.30 seconds cold and 0.144 seconds warm; a diagnostic took 0.038 and 0.009 seconds. Those are small-gate timings, not a full-year throughput forecast. Scope-specific query pins were also checked by evicting 141 unrelated blocks while retaining 21 pinned blocks, then evicting the 21 after release.

The fresh `v3_zoom_gate_complete` real-core run executed all nine requested variant resolutions, published six candidates and six dossiers, and finished in 98 seconds. The earlier bounded zoom attempts revealed and fixed sparse worker-partition counting, restored-cache dependencies and variant-only packed-bin materialization. Those failed attempts are retained as evidence of the repair path, not counted as passing gates.

The fresh `v3_zoom_on_request_gate` comprehensive run completed 162/162 mandatory subgroup tasks, executed all nine requested variant resolutions, selected six candidates and built zero broad dossiers as configured. Independent diagnostics and on-demand dossier assembly remain available through `research inspect`.

The fresh `v3_fused_resume_gate` bounded run completed 162/162 mandatory subgroup tasks with stable core tile identities. A focused numerical test produced matching r3/r5/r10 surface sums with runtime pair caps of one and three, and reused the committed logical tile under a changed cap.

The focused `v3_global_profile_gate` completed 36/36 mandatory subgroup tasks with one explicit global-rank feature and one local feature. Its global callback used one chunk, 1.823 seconds of feature construction, 0.015 seconds of panel reads and 430 MB peak process RSS. This identifies construction as the local cost, while leaving overlapping-history behavior on longer inputs unmeasured.
