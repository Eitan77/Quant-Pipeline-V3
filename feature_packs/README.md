# Feature packs

Each pack declares identity, implementation, causal availability, price basis,
history requirements and canonical concept mapping. Experimental packs remain
separate from the built-in V2 parity port and cannot authorize sealed data.

